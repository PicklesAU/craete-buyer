import { PageViewTelemetry } from './page-view-telemetry';

describe('PageViewTelemetry', () => {
  it('should create an instance', () => {
    expect(new PageViewTelemetry()).toBeTruthy();
  });
});
